


// empty callbacks


void item_spawn_UNKNOWN(World* w, PartInstance* pi, void* info) {}
void item_remove_UNKNOWN(World* w, PartInstance* pi) {}
void item_move_UNKNOWN(World* w, PartInstance* pi, Vector* newPos) {}

void item_spawn_ITEM(World* w, PartInstance* pi, void* info) {}
void item_remove_ITEM(World* w, PartInstance* pi) {}
void item_move_ITEM(World* w, PartInstance* pi, Vector* newPos) {}

void item_spawn_STATICMESH(World* w, PartInstance* pi, void* info) {}
void item_remove_STATICMESH(World* w, PartInstance* pi) {}
void item_move_STATICMESH(World* w, PartInstance* pi, Vector* newPos) {}



// waiting to be moved to their appropriate files


void item_spawn_DYNAMICMESH(World* w, PartInstance* pi, void* info) {
	
}
void item_remove_DYNAMICMESH(World* w, PartInstance* pi) {
	
}
void item_move_DYNAMICMESH(World* w, PartInstance* pi, Vector* newPos) {
	
}


void item_spawn_EMITTER(World* w, PartInstance* pi, void* info) {
	
}
void item_remove_EMITTER(World* w, PartInstance* pi) {
	
}
void item_move_EMITTER(World* w, PartInstance* pi, Vector* newPos) {
	
}


void item_spawn_LIGHT(World* w, PartInstance* pi, void* info) {
	
}
void item_remove_LIGHT(World* w, PartInstance* pi) {
	
}
void item_move_LIGHT(World* w, PartInstance* pi, Vector* newPos) {
	
}


void item_spawn_DECAL(World* w, PartInstance* pi, void* info) {
	
}
void item_remove_DECAL(World* w, PartInstance* pi) {
	
}
void item_move_DECAL(World* w, PartInstance* pi, Vector* newPos) {
	
}



void item_spawn_MARKER(World* w, PartInstance* pi, void* info) {
	
}
void item_remove_MARKER(World* w, PartInstance* pi) {
	
}
void item_move_MARKER(World* w, PartInstance* pi, Vector* newPos) {
	
}


void item_spawn_SOUNDCLIP(World* w, PartInstance* pi, void* info) {
	
}
void item_remove_SOUNDCLIP(World* w, PartInstance* pi) {
	
}
void item_move_SOUNDCLIP(World* w, PartInstance* pi, Vector* newPos) {
	
}














